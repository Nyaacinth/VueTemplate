{
    "configurations": [
        {
            "name": "Launch App in Development Mode",
            "type": "node-terminal",
            "request": "launch",
            "command": "bun run launch-app-dev"
        }
    ]
}
